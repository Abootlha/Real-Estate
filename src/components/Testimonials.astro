---
const testimonials = [
    {
        content:
            "HomeDaze made finding my apartment in Bangalore so easy! The roommate matching was spot-on, and I'm now living with amazing flatmates.",
        author: "Shruti Menon",
        role: "Software Engineer at Razorpay",
        image: "https://images.unsplash.com/photo-1494790108377-be9c29b29330?w=100&h=100&fit=crop&q=90",
        rating: 5,
    },
    {
        content:
            "The verified listings saved me so much time. No more fake photos or misleading descriptions. Found my dream 2BHK within a week!",
        author: "Karthik Nair",
        role: "Product Manager at Flipkart",
        image: "https://images.unsplash.com/photo-1507003211169-0a1dd7228f2d?w=100&h=100&fit=crop&q=90",
        rating: 5,
    },
    {
        content:
            "As a first-time home buyer, the comparison tools and market insights from the Premium plan were invaluable. 100% worth the upgrade.",
        author: "Anisha Kapoor",
        role: "Marketing Director",
        image: "https://images.unsplash.com/photo-1438761681033-6461ffad8d80?w=100&h=100&fit=crop&q=90",
        rating: 5,
    },
];
---

<section class="section testimonials-section">
    <div class="container">
        <div class="section-header" data-animate="fade-up">
            <span class="section-eyebrow">Testimonials</span>
            <h2 class="section-title">
                Loved by <em class="text-coral">Thousands</em>
            </h2>
            <p class="section-subtitle">
                See what our happy residents are saying about their HomeDaze
                experience
            </p>
        </div>

        <div class="testimonials-grid">
            {
                testimonials.map((testimonial, index) => (
                    <article
                        class="testimonial-card"
                        data-animate="fade-up"
                        data-delay={index + 1}
                    >
                        <div class="card-rating">
                            {[...Array(testimonial.rating)].map(() => (
                                <svg
                                    width="18"
                                    height="18"
                                    viewBox="0 0 24 24"
                                    fill="currentColor"
                                >
                                    <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2" />
                                </svg>
                            ))}
                        </div>
                        <blockquote class="card-content">
                            "{testimonial.content}"
                        </blockquote>
                        <div class="card-author">
                            <img
                                src={testimonial.image}
                                alt={testimonial.author}
                                class="author-image"
                            />
                            <div class="author-info">
                                <span class="author-name">
                                    {testimonial.author}
                                </span>
                                <span class="author-role">
                                    {testimonial.role}
                                </span>
                            </div>
                        </div>
                    </article>
                ))
            }
        </div>

        <!-- Trust Logos -->
        <div class="trust-logos" data-animate="fade-up">
            <p class="trust-label">Trusted by residents from</p>
            <div class="logos">
                <span class="logo-item">Google</span>
                <span class="logo-item">Microsoft</span>
                <span class="logo-item">Amazon</span>
                <span class="logo-item">Flipkart</span>
                <span class="logo-item">Swiggy</span>
                <span class="logo-item">Zomato</span>
            </div>
        </div>
    </div>
</section>

<style>
    .testimonials-section {
        background: var(--bg-secondary);
    }

    .section-header {
        text-align: center;
        margin-bottom: 60px;
    }

    .section-eyebrow {
        display: inline-block;
        font-size: var(--text-sm);
        font-weight: 600;
        color: var(--accent);
        text-transform: uppercase;
        letter-spacing: 0.1em;
        margin-bottom: 16px;
    }

    .section-title {
        font-family: var(--font-display);
        font-size: clamp(2rem, 4vw, var(--text-5xl));
        font-style: italic;
        color: var(--text-primary);
        margin-bottom: 16px;
        line-height: 1.1;
    }

    .section-subtitle {
        font-size: var(--text-lg);
        color: var(--text-secondary);
        max-width: 500px;
        margin: 0 auto;
        line-height: 1.6;
    }

    /* Testimonials Grid */
    .testimonials-grid {
        display: grid;
        grid-template-columns: repeat(3, 1fr);
        gap: 24px;
        margin-bottom: 60px;
    }

    /* Testimonial Card */
    .testimonial-card {
        background: var(--glass-bg);
        backdrop-filter: var(--glass-blur);
        -webkit-backdrop-filter: var(--glass-blur);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-xl);
        padding: 32px;
        transition: all 0.4s var(--ease-out);
    }

    .testimonial-card:hover {
        transform: translateY(-4px);
        box-shadow: var(--shadow-lg);
        border-color: rgba(255, 255, 255, 0.12);
    }

    .card-rating {
        display: flex;
        gap: 4px;
        color: var(--color-warning);
        margin-bottom: 20px;
    }

    .card-content {
        font-size: var(--text-base);
        color: var(--text-primary);
        line-height: 1.7;
        margin-bottom: 24px;
        font-style: italic;
    }

    .card-author {
        display: flex;
        align-items: center;
        gap: 14px;
    }

    .author-image {
        width: 48px;
        height: 48px;
        border-radius: 50%;
        object-fit: cover;
        border: 2px solid var(--glass-border);
    }

    .author-info {
        display: flex;
        flex-direction: column;
        gap: 2px;
    }

    .author-name {
        font-size: var(--text-sm);
        font-weight: 600;
        color: var(--text-primary);
    }

    .author-role {
        font-size: var(--text-xs);
        color: var(--text-secondary);
    }

    /* Trust Logos */
    .trust-logos {
        text-align: center;
    }

    .trust-label {
        font-size: var(--text-sm);
        color: var(--text-muted);
        margin-bottom: 20px;
    }

    .logos {
        display: flex;
        justify-content: center;
        gap: 40px;
        flex-wrap: wrap;
    }

    .logo-item {
        font-family: var(--font-body);
        font-size: var(--text-lg);
        font-weight: 700;
        color: var(--text-muted);
        opacity: 0.5;
        transition: opacity 0.3s;
    }

    .logo-item:hover {
        opacity: 1;
    }

    /* Responsive */
    @media (max-width: 1024px) {
        .testimonials-grid {
            grid-template-columns: 1fr;
            max-width: 600px;
            margin: 0 auto 60px;
        }
    }

    @media (max-width: 768px) {
        .logos {
            gap: 24px;
        }

        .logo-item {
            font-size: var(--text-base);
        }
    }
</style>
