---
interface Props {
    transparent?: boolean;
}

const { transparent = false } = Astro.props;
---

<!-- Top Utility Bar -->
<div class="utility-bar">
    <div class="container">
        <div class="utility-inner">
            <div class="utility-left">
                <a href="tel:+919876543210" class="utility-link">
                    <svg
                        width="14"
                        height="14"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <path
                            d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72 12.84 12.84 0 00.7 2.81 2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45 12.84 12.84 0 002.81.7A2 2 0 0122 16.92z"
                        ></path>
                    </svg>
                    <span>+91 98765 43210</span>
                </a>
                <span class="utility-divider"></span>
                <a href="mailto:hello@homedaze.in" class="utility-link">
                    <svg
                        width="14"
                        height="14"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <path
                            d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"
                        ></path>
                        <polyline points="22,6 12,13 2,6"></polyline>
                    </svg>
                    <span>hello@homedaze.in</span>
                </a>
            </div>
            <div class="utility-right">
                <a href="#" class="utility-link app-link">
                    <svg
                        width="14"
                        height="14"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <rect x="5" y="2" width="14" height="20" rx="2" ry="2"
                        ></rect>
                        <line x1="12" y1="18" x2="12.01" y2="18"></line>
                    </svg>
                    <span>Get App</span>
                </a>
                <span class="utility-divider"></span>
                <a href="/contact" class="utility-link">
                    <svg
                        width="14"
                        height="14"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <circle cx="12" cy="12" r="10"></circle>
                        <path d="M9.09 9a3 3 0 015.83 1c0 2-3 3-3 3"></path>
                        <line x1="12" y1="17" x2="12.01" y2="17"></line>
                    </svg>
                    <span>Help</span>
                </a>
            </div>
        </div>
    </div>
</div>

<!-- Main Navigation -->
<nav class={`navbar ${transparent ? "navbar-transparent" : ""}`}>
    <div class="container">
        <div class="nav-inner">
            <!-- Logo -->
            <a href="/" class="logo">
                <div class="logo-icon">
                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none">
                        <path d="M12 2L2 7V10H22V7L12 2Z" fill="currentColor"
                        ></path>
                        <path
                            d="M4 12V22H10V16H14V22H20V12H4Z"
                            fill="currentColor"></path>
                    </svg>
                </div>
                <span class="logo-text"
                    >Home<span class="logo-accent">Daze</span></span
                >
            </a>

            <!-- Search Bar (Desktop) -->
            <div class="nav-search">
                <svg
                    width="18"
                    height="18"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                >
                    <circle cx="11" cy="11" r="8"></circle>
                    <path d="M21 21l-4.35-4.35"></path>
                </svg>
                <input
                    type="text"
                    placeholder="Search properties, cities, roommates..."
                    class="search-input"
                />
                <button class="search-btn-inline">
                    <svg
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                        <polyline points="12 5 19 12 12 19"></polyline>
                    </svg>
                </button>
            </div>

            <!-- Nav Links -->
            <div class="nav-links">
                <div class="nav-item has-dropdown">
                    <button class="nav-link">
                        Properties
                        <svg
                            width="12"
                            height="12"
                            viewBox="0 0 24 24"
                            fill="none"
                            stroke="currentColor"
                            stroke-width="2"
                        >
                            <polyline points="6 9 12 15 18 9"></polyline>
                        </svg>
                    </button>
                    <div class="dropdown-menu">
                        <div class="dropdown-grid">
                            <div class="dropdown-section">
                                <h4 class="dropdown-title">By Type</h4>
                                <a
                                    href="/properties?type=apartment"
                                    class="dropdown-link"
                                >
                                    <span class="dropdown-icon">🏢</span>
                                    <div>
                                        <span class="dropdown-text"
                                            >Apartments</span
                                        >
                                        <span class="dropdown-desc"
                                            >Flats & studio apartments</span
                                        >
                                    </div>
                                </a>
                                <a
                                    href="/properties?type=house"
                                    class="dropdown-link"
                                >
                                    <span class="dropdown-icon">🏠</span>
                                    <div>
                                        <span class="dropdown-text">Houses</span
                                        >
                                        <span class="dropdown-desc"
                                            >Independent houses</span
                                        >
                                    </div>
                                </a>
                                <a
                                    href="/properties?type=villa"
                                    class="dropdown-link"
                                >
                                    <span class="dropdown-icon">🏡</span>
                                    <div>
                                        <span class="dropdown-text">Villas</span
                                        >
                                        <span class="dropdown-desc"
                                            >Luxury villas & bungalows</span
                                        >
                                    </div>
                                </a>
                                <a
                                    href="/properties?type=pg"
                                    class="dropdown-link"
                                >
                                    <span class="dropdown-icon">🛏️</span>
                                    <div>
                                        <span class="dropdown-text"
                                            >PG/Hostels</span
                                        >
                                        <span class="dropdown-desc"
                                            >Paying guest & hostels</span
                                        >
                                    </div>
                                </a>
                            </div>
                            <div class="dropdown-section">
                                <h4 class="dropdown-title">Popular Cities</h4>
                                <a
                                    href="/city/mumbai"
                                    class="dropdown-link compact">Mumbai</a
                                >
                                <a
                                    href="/city/bangalore"
                                    class="dropdown-link compact">Bangalore</a
                                >
                                <a
                                    href="/city/delhi-ncr"
                                    class="dropdown-link compact">Delhi NCR</a
                                >
                                <a
                                    href="/city/hyderabad"
                                    class="dropdown-link compact">Hyderabad</a
                                >
                                <a
                                    href="/city/pune"
                                    class="dropdown-link compact">Pune</a
                                >
                                <a
                                    href="/city/chennai"
                                    class="dropdown-link compact">Chennai</a
                                >
                            </div>
                        </div>
                    </div>
                </div>
                <a href="/roommates" class="nav-link">Roommates</a>
                <a href="/pricing" class="nav-link">Pricing</a>
                <a href="/about" class="nav-link">About</a>
            </div>

            <!-- Nav Actions -->
            <div class="nav-actions">
                <a href="/properties/add" class="btn btn-outline btn-sm">
                    <svg
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <line x1="12" y1="5" x2="12" y2="19"></line>
                        <line x1="5" y1="12" x2="19" y2="12"></line>
                    </svg>
                    Post Property
                </a>
                <a href="/login" class="btn btn-ghost btn-sm">Login</a>
                <a href="/signup" class="btn btn-primary btn-sm">Sign Up</a>
            </div>

            <!-- Mobile Toggle -->
            <button class="mobile-toggle" aria-label="Menu" id="mobileToggle">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </div>
</nav>

<!-- Mobile Menu Drawer -->
<div class="mobile-drawer" id="mobileDrawer">
    <div class="drawer-backdrop"></div>
    <div class="drawer-content">
        <div class="drawer-header">
            <a href="/" class="logo">
                <div class="logo-icon">
                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none">
                        <path d="M12 2L2 7V10H22V7L12 2Z" fill="currentColor"
                        ></path>
                        <path
                            d="M4 12V22H10V16H14V22H20V12H4Z"
                            fill="currentColor"></path>
                    </svg>
                </div>
                <span class="logo-text"
                    >Home<span class="logo-accent">Daze</span></span
                >
            </a>
            <button class="drawer-close" id="drawerClose">
                <svg
                    width="24"
                    height="24"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                >
                    <line x1="18" y1="6" x2="6" y2="18"></line>
                    <line x1="6" y1="6" x2="18" y2="18"></line>
                </svg>
            </button>
        </div>

        <div class="drawer-search">
            <svg
                width="18"
                height="18"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
            >
                <circle cx="11" cy="11" r="8"></circle>
                <path d="M21 21l-4.35-4.35"></path>
            </svg>
            <input type="text" placeholder="Search..." class="search-input" />
        </div>

        <nav class="drawer-nav">
            <a href="/" class="drawer-link active">Home</a>
            <div class="drawer-accordion">
                <button class="drawer-link accordion-toggle">
                    Properties
                    <svg
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <polyline points="6 9 12 15 18 9"></polyline>
                    </svg>
                </button>
                <div class="accordion-content">
                    <a href="/properties?type=apartment" class="drawer-sublink"
                        >Apartments</a
                    >
                    <a href="/properties?type=house" class="drawer-sublink"
                        >Houses</a
                    >
                    <a href="/properties?type=villa" class="drawer-sublink"
                        >Villas</a
                    >
                    <a href="/properties?type=pg" class="drawer-sublink"
                        >PG/Hostels</a
                    >
                </div>
            </div>
            <a href="/roommates" class="drawer-link">Roommates</a>
            <a href="/pricing" class="drawer-link">Pricing</a>
            <a href="/about" class="drawer-link">About</a>
            <a href="/contact" class="drawer-link">Contact</a>
        </nav>

        <div class="drawer-actions">
            <a href="/properties/add" class="btn btn-outline btn-block">
                <svg
                    width="16"
                    height="16"
                    viewBox="0 0 24 24"
                    fill="none"
                    stroke="currentColor"
                    stroke-width="2"
                >
                    <line x1="12" y1="5" x2="12" y2="19"></line>
                    <line x1="5" y1="12" x2="19" y2="12"></line>
                </svg>
                Post Property Free
            </a>
            <a href="/login" class="btn btn-secondary btn-block">Login</a>
            <a href="/signup" class="btn btn-primary btn-block">Sign Up</a>
        </div>

        <div class="drawer-footer">
            <div class="drawer-contact">
                <a href="tel:+919876543210" class="drawer-contact-link">
                    <svg
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <path
                            d="M22 16.92v3a2 2 0 01-2.18 2 19.79 19.79 0 01-8.63-3.07 19.5 19.5 0 01-6-6 19.79 19.79 0 01-3.07-8.67A2 2 0 014.11 2h3a2 2 0 012 1.72 12.84 12.84 0 00.7 2.81 2 2 0 01-.45 2.11L8.09 9.91a16 16 0 006 6l1.27-1.27a2 2 0 012.11-.45 12.84 12.84 0 002.81.7A2 2 0 0122 16.92z"
                        ></path>
                    </svg>
                    +91 98765 43210
                </a>
                <a href="mailto:hello@homedaze.in" class="drawer-contact-link">
                    <svg
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                    >
                        <path
                            d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"
                        ></path>
                        <polyline points="22,6 12,13 2,6"></polyline>
                    </svg>
                    hello@homedaze.in
                </a>
            </div>
            <div class="drawer-app">
                <span>Download App</span>
                <div class="app-badges">
                    <a href="#" class="app-badge">
                        <svg
                            width="20"
                            height="20"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                        >
                            <path
                                d="M17.05 20.28c-.98.95-2.05.8-3.08.35-1.09-.46-2.09-.48-3.24 0-1.44.62-2.2.44-3.06-.35C2.79 15.25 3.51 7.59 9.05 7.31c1.35.07 2.29.74 3.08.8 1.18-.24 2.31-.93 3.57-.84 1.51.12 2.65.72 3.4 1.8-3.12 1.87-2.38 5.98.48 7.13-.57 1.5-1.31 2.99-2.54 4.09l.01-.01zM12.03 7.25c-.15-2.23 1.66-4.07 3.74-4.25.29 2.58-2.34 4.5-3.74 4.25z"
                            ></path>
                        </svg>
                    </a>
                    <a href="#" class="app-badge">
                        <svg
                            width="20"
                            height="20"
                            viewBox="0 0 24 24"
                            fill="currentColor"
                        >
                            <path
                                d="M3.609 1.814L13.792 12 3.61 22.186a.996.996 0 01-.61-.92V2.734a1 1 0 01.609-.92zm10.89 10.893l2.302 2.302-10.937 6.333 8.635-8.635zm3.199-3.198l2.807 1.626a1 1 0 010 1.73l-2.808 1.626L15.206 12l2.492-2.491zM5.864 2.658L16.8 8.99l-2.302 2.302-8.634-8.634z"
                            ></path>
                        </svg>
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    /* Utility Bar */
    .utility-bar {
        background: var(--bg-secondary);
        border-bottom: 1px solid var(--glass-border);
        padding: 8px 0;
        font-size: var(--text-xs);
    }

    .utility-inner {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .utility-left,
    .utility-right {
        display: flex;
        align-items: center;
        gap: 16px;
    }

    .utility-link {
        display: flex;
        align-items: center;
        gap: 6px;
        color: var(--text-secondary);
        transition: color 0.3s var(--ease-out);
    }

    .utility-link:hover {
        color: var(--accent);
    }

    .utility-link svg {
        opacity: 0.7;
    }

    .utility-divider {
        width: 1px;
        height: 12px;
        background: var(--glass-border);
    }

    .app-link {
        color: var(--accent);
        font-weight: 500;
    }

    /* Main Navbar */
    .navbar {
        position: sticky;
        top: 0;
        left: 0;
        right: 0;
        z-index: 1000;
        padding: 12px 0;
        background: var(--bg-primary);
        border-bottom: 1px solid var(--glass-border);
        transition: all 0.4s var(--ease-expo);
    }

    .navbar.scrolled {
        background: rgba(250, 244, 239, 0.95);
        backdrop-filter: blur(20px);
        -webkit-backdrop-filter: blur(20px);
        padding: 10px 0;
    }

    .nav-inner {
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: 24px;
    }

    /* Logo */
    .logo {
        display: flex;
        align-items: center;
        gap: 10px;
        flex-shrink: 0;
    }

    .logo-icon {
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--accent);
        border-radius: var(--radius-md);
        color: var(--text-inverse);
        transition: all 0.4s var(--ease-spring);
        box-shadow: 0 4px 16px var(--color-coral-glow);
    }

    .logo:hover .logo-icon {
        transform: rotate(-5deg) scale(1.05);
    }

    .logo-text {
        font-family: var(--font-display);
        font-size: var(--text-lg);
        font-weight: 400;
        font-style: italic;
        color: var(--text-primary);
    }

    .logo-accent {
        color: var(--accent);
    }

    /* Nav Search */
    .nav-search {
        flex: 1;
        max-width: 420px;
        display: flex;
        align-items: center;
        gap: 12px;
        background: var(--bg-elevated);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-full);
        padding: 10px 16px;
        transition: all 0.3s var(--ease-out);
    }

    .nav-search:focus-within {
        border-color: var(--accent);
        box-shadow: 0 0 0 3px var(--color-coral-subtle);
    }

    .nav-search svg {
        color: var(--text-muted);
        flex-shrink: 0;
    }

    .nav-search .search-input {
        flex: 1;
        background: transparent;
        border: none;
        font-size: var(--text-sm);
        color: var(--text-primary);
        outline: none;
    }

    .nav-search .search-input::placeholder {
        color: var(--text-muted);
    }

    .search-btn-inline {
        width: 32px;
        height: 32px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--accent);
        border-radius: 50%;
        color: var(--text-inverse);
        transition: all 0.3s var(--ease-out);
    }

    .search-btn-inline:hover {
        background: var(--accent-hover);
        transform: scale(1.05);
    }

    /* Nav Links */
    .nav-links {
        display: flex;
        align-items: center;
        gap: 4px;
    }

    .nav-link {
        display: flex;
        align-items: center;
        gap: 4px;
        padding: 10px 16px;
        font-size: var(--text-sm);
        font-weight: 500;
        color: var(--text-secondary);
        border-radius: var(--radius-md);
        transition: all 0.3s var(--ease-out);
        background: transparent;
        border: none;
        cursor: pointer;
    }

    .nav-link:hover {
        color: var(--text-primary);
        background: var(--bg-elevated);
    }

    /* Dropdown */
    .nav-item {
        position: relative;
    }

    .has-dropdown .nav-link svg {
        transition: transform 0.3s var(--ease-out);
    }

    .has-dropdown:hover .nav-link svg {
        transform: rotate(180deg);
    }

    .dropdown-menu {
        position: absolute;
        top: 100%;
        left: 0;
        min-width: 480px;
        background: var(--bg-secondary);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-xl);
        padding: 20px;
        box-shadow: var(--shadow-xl);
        opacity: 0;
        visibility: hidden;
        transform: translateY(10px);
        transition: all 0.3s var(--ease-out);
        z-index: 100;
    }

    .has-dropdown:hover .dropdown-menu {
        opacity: 1;
        visibility: visible;
        transform: translateY(0);
    }

    .dropdown-grid {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 24px;
    }

    .dropdown-title {
        font-size: var(--text-xs);
        font-weight: 600;
        color: var(--text-muted);
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: 12px;
        padding-left: 8px;
    }

    .dropdown-link {
        display: flex;
        align-items: flex-start;
        gap: 12px;
        padding: 10px 8px;
        border-radius: var(--radius-md);
        transition: all 0.2s var(--ease-out);
    }

    .dropdown-link:hover {
        background: var(--bg-elevated);
    }

    .dropdown-link.compact {
        padding: 8px;
        font-size: var(--text-sm);
        color: var(--text-secondary);
    }

    .dropdown-link.compact:hover {
        color: var(--accent);
    }

    .dropdown-icon {
        font-size: 20px;
    }

    .dropdown-text {
        display: block;
        font-size: var(--text-sm);
        font-weight: 500;
        color: var(--text-primary);
    }

    .dropdown-desc {
        display: block;
        font-size: var(--text-xs);
        color: var(--text-muted);
        margin-top: 2px;
    }

    /* Nav Actions */
    .nav-actions {
        display: flex;
        align-items: center;
        gap: 8px;
    }

    /* Mobile Toggle */
    .mobile-toggle {
        display: none;
        flex-direction: column;
        justify-content: center;
        gap: 5px;
        width: 44px;
        height: 44px;
        padding: 12px;
        background: var(--bg-elevated);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-md);
        cursor: pointer;
    }

    .mobile-toggle span {
        display: block;
        height: 2px;
        background: var(--text-primary);
        border-radius: 2px;
        transition: all 0.3s var(--ease-out);
    }

    .mobile-toggle span:nth-child(2) {
        width: 60%;
    }

    .mobile-toggle:hover span {
        background: var(--accent);
    }

    .mobile-toggle.active span:nth-child(1) {
        transform: rotate(45deg) translate(5px, 5px);
    }

    .mobile-toggle.active span:nth-child(2) {
        opacity: 0;
    }

    .mobile-toggle.active span:nth-child(3) {
        transform: rotate(-45deg) translate(5px, -5px);
    }

    /* Mobile Drawer */
    .mobile-drawer {
        position: fixed;
        inset: 0;
        z-index: 9999;
        visibility: hidden;
    }

    .mobile-drawer.open {
        visibility: visible;
    }

    .drawer-backdrop {
        position: absolute;
        inset: 0;
        background: rgba(0, 0, 0, 0.6);
        opacity: 0;
        transition: opacity 0.4s var(--ease-out);
    }

    .mobile-drawer.open .drawer-backdrop {
        opacity: 1;
    }

    .drawer-content {
        position: absolute;
        top: 0;
        right: 0;
        width: 100%;
        max-width: 360px;
        height: 100%;
        background: var(--bg-secondary);
        transform: translateX(100%);
        transition: transform 0.4s var(--ease-expo);
        display: flex;
        flex-direction: column;
        overflow-y: auto;
    }

    .mobile-drawer.open .drawer-content {
        transform: translateX(0);
    }

    .drawer-header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 16px 20px;
        border-bottom: 1px solid var(--glass-border);
    }

    .drawer-close {
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--bg-elevated);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-md);
        color: var(--text-secondary);
        transition: all 0.3s var(--ease-out);
    }

    .drawer-close:hover {
        color: var(--accent);
        border-color: var(--accent);
    }

    .drawer-search {
        display: flex;
        align-items: center;
        gap: 12px;
        margin: 16px 20px;
        padding: 12px 16px;
        background: var(--bg-elevated);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-lg);
    }

    .drawer-search svg {
        color: var(--text-muted);
        flex-shrink: 0;
    }

    .drawer-search .search-input {
        flex: 1;
        background: transparent;
        border: none;
        font-size: var(--text-sm);
        color: var(--text-primary);
        outline: none;
    }

    .drawer-nav {
        flex: 1;
        padding: 8px 20px;
    }

    .drawer-link {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 14px 0;
        font-size: var(--text-base);
        font-weight: 500;
        color: var(--text-secondary);
        border-bottom: 1px solid var(--glass-border);
        transition: color 0.3s var(--ease-out);
        background: transparent;
        width: 100%;
        text-align: left;
    }

    .drawer-link:hover,
    .drawer-link.active {
        color: var(--accent);
    }

    .drawer-accordion .accordion-content {
        display: none;
        padding-left: 16px;
    }

    .drawer-accordion.open .accordion-content {
        display: block;
    }

    .drawer-accordion.open .accordion-toggle svg {
        transform: rotate(180deg);
    }

    .drawer-sublink {
        display: block;
        padding: 10px 0;
        font-size: var(--text-sm);
        color: var(--text-muted);
        border-bottom: 1px solid var(--glass-border);
    }

    .drawer-sublink:hover {
        color: var(--accent);
    }

    .drawer-actions {
        padding: 20px;
        display: flex;
        flex-direction: column;
        gap: 10px;
        border-top: 1px solid var(--glass-border);
    }

    .btn-block {
        width: 100%;
        justify-content: center;
    }

    .drawer-footer {
        padding: 20px;
        background: var(--bg-primary);
        border-top: 1px solid var(--glass-border);
    }

    .drawer-contact {
        display: flex;
        flex-direction: column;
        gap: 12px;
        margin-bottom: 16px;
    }

    .drawer-contact-link {
        display: flex;
        align-items: center;
        gap: 10px;
        font-size: var(--text-sm);
        color: var(--text-secondary);
    }

    .drawer-contact-link:hover {
        color: var(--accent);
    }

    .drawer-app {
        display: flex;
        align-items: center;
        justify-content: space-between;
    }

    .drawer-app span {
        font-size: var(--text-sm);
        color: var(--text-muted);
    }

    .app-badges {
        display: flex;
        gap: 12px;
    }

    .app-badge {
        width: 36px;
        height: 36px;
        display: flex;
        align-items: center;
        justify-content: center;
        background: var(--bg-elevated);
        border: 1px solid var(--glass-border);
        border-radius: var(--radius-md);
        color: var(--text-secondary);
        transition: all 0.3s var(--ease-out);
    }

    .app-badge:hover {
        color: var(--accent);
        border-color: var(--accent);
    }

    /* Responsive */
    @media (max-width: 1200px) {
        .nav-search {
            max-width: 300px;
        }
    }

    @media (max-width: 1024px) {
        .utility-bar {
            display: none;
        }

        .nav-search,
        .nav-links,
        .nav-actions {
            display: none;
        }

        .mobile-toggle {
            display: flex;
        }

        .navbar {
            position: fixed;
            top: 0;
        }
    }

    @media (max-width: 480px) {
        .drawer-content {
            max-width: 100%;
        }
    }
</style>

<script>
    // Mobile menu toggle
    const mobileToggle = document.getElementById("mobileToggle");
    const mobileDrawer = document.getElementById("mobileDrawer");
    const drawerClose = document.getElementById("drawerClose");
    const drawerBackdrop = document.querySelector(".drawer-backdrop");

    function openDrawer() {
        mobileDrawer?.classList.add("open");
        mobileToggle?.classList.add("active");
        document.body.style.overflow = "hidden";
    }

    function closeDrawer() {
        mobileDrawer?.classList.remove("open");
        mobileToggle?.classList.remove("active");
        document.body.style.overflow = "";
    }

    mobileToggle?.addEventListener("click", openDrawer);
    drawerClose?.addEventListener("click", closeDrawer);
    drawerBackdrop?.addEventListener("click", closeDrawer);

    // Accordion toggles
    const accordionToggles = document.querySelectorAll(".accordion-toggle");
    accordionToggles.forEach((toggle) => {
        toggle.addEventListener("click", () => {
            const accordion = toggle.parentElement;
            accordion?.classList.toggle("open");
        });
    });

    // Navbar scroll effect
    const navbar = document.querySelector(".navbar");
    window.addEventListener("scroll", () => {
        if (window.scrollY > 50) {
            navbar?.classList.add("scrolled");
        } else {
            navbar?.classList.remove("scrolled");
        }
    });
</script>
